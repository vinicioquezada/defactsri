export class Privilegio {
    privilegio: string;
    verificar: string;

    constructor(privilegio: string, verificar: string) {
        this.privilegio = privilegio;
        this.verificar = verificar;
    }
}